'use strict';

/**
 * promotion-activity service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::promotion-activity.promotion-activity');
