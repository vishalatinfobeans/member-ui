'use strict';

/**
 * promotion-activity router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::promotion-activity.promotion-activity');
